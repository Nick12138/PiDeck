name: Release desktop installers

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing tag to release (e.g. v0.1.1)"
        required: true

permissions:
  contents: write

concurrency:
  group: release-${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}
  cancel-in-progress: false

jobs:
  build-release:
    name: Build Windows x64
    runs-on: windows-2022
    timeout-minutes: 75
    env:
      TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
      TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
      RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: pnpm
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri
      - uses: actions/cache@v4
        with:
          path: .runtime-cache
          key: ${{ runner.os }}-${{ runner.arch }}-release-runtime-${{ hashFiles('scripts/release-runtime.lock.json') }}
      - run: pnpm install --frozen-lockfile
      - run: pnpm verify:quick
      - run: pnpm package:release
      - name: Stage accepted platform assets
        shell: bash
        run: node scripts/generate-update-manifest.mjs --stage-platform --tag "$RELEASE_TAG"
      - uses: actions/upload-artifact@v4
        with:
          name: release-platform-windows-x64
          path: apps/desktop/src-tauri/target/release-staging/github-release-platform
          if-no-files-found: error
          retention-days: 7

  create-release:
    name: Create draft release
    needs: build-release
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      GH_TOKEN: ${{ github.token }}
      RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}
      - uses: actions/download-artifact@v4
        with:
          pattern: release-platform-*
          path: release-input
          merge-multiple: true
      - name: Generate updater feed
        shell: bash
        run: |
          node scripts/generate-update-manifest.mjs \
            --tag "$RELEASE_TAG" \
            --input-dir release-input \
            --output-dir release-output
      - name: Create or update draft GitHub release
        shell: bash
        run: |
          set -euo pipefail
          assets=(release-output/*)
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release upload "$RELEASE_TAG" --clobber "${assets[@]}"
            gh release edit "$RELEASE_TAG" --draft --title "PiDeck $RELEASE_TAG"
          else
            gh release create "$RELEASE_TAG" \
              --draft \
              --title "PiDeck $RELEASE_TAG" \
              --generate-notes \
              "${assets[@]}"
          fi
