# 插件库配置项：视觉模型动态单选框 — 现状与待完善清单

> 本任务让 PiDeck 端支持插件库配置项的 `optionsSource` 字段，先期把"默认视觉模型"，
> 渲染成由本机运行时模型列表动态生成的"供应商名 · 模型名"单选下拉。
> 本文记录当前已落盘的改动、未完成事项、以及接续者要做的优先事项。

---

## 1. 任务三处改动总览

| # | 范围 | 状态 | 关键字 |
|---|------|------|-------|
| 1 | host：`modelSummaries` 补 `input` 字段 | ✅ 已落盘 | `packages/pi-host/src/pi-settings-controller.ts` |
| 2 | host：插件库清单校验放宽 `optionsSource` | ✅ 已落盘 | `packages/pi-host/src/plugin-library-catalog.ts` |
| 3 | 前端：渲染"视觉模型"动态单选框 | ✅ 已落盘 | `apps/desktop/src/features/plugin-library/PluginLibraryPage.tsx` |
| 附 | protocol 类型 + DTO 校验（前端要走通校验，必改） | ✅ 已落盘 | `packages/protocol/src/types.ts` / `dto-validate.ts` |
| 附 | host：plugin-library-catalog 新增测试用例 | ✅ 已落盘 | `packages/pi-host/src/plugin-library-catalog.test.ts` |
| 附 | host：新增 `pi-settings-controller.test.ts` | ✅ 已落盘 | `packages/pi-host/src/pi-settings-controller.test.ts` |
| 附 | 前端：`isVisionCapable` / `visionModelOption` / `wantsVisionModelOptions` 纯函数 + 单测 | ✅ 已落盘 | `apps/desktop/src/features/plugin-library/plugin-library-model.ts` / `.test.ts` |
| 附 | i18n：`pluginsVisionModelAuto` / `pluginsVisionModelFallbackHelp` | ✅ 已落盘 | `apps/desktop/src/lib/i18n/{en,zh}.ts` |
| 4 | 前端：DOM 验收测试（`PluginLibraryPage.dom.test.tsx`） | ✅ 已完成 | 覆盖动态视觉模型选项、自动选择、回退输入、未知来源 |
| 5 | 端到端：实际跑一遍 `pnpm test` / `pnpm typecheck` | 🟡 部分完成 | `pnpm typecheck` 通过；相关测试通过；全量 `pnpm test` 被既有 host 集成测试超时阻断 |

---

## 2. 已落盘代码速览

### 2.1 pi-host：`modelSummaries` 补 `input`
`packages/pi-host/src/pi-settings-controller.ts:41-50`

```ts
function modelSummaries(runtime: ModelRuntime): ModelSummary[] {
  return runtime.getAvailableSnapshot().map((model) => ({
    provider: model.provider,
    providerName: runtime.getProvider(model.provider)?.name,
    modelId: model.id,
    name: model.name ?? model.id,
    input: model.input ?? [],          // ← 新增；缺失时给空数组
  }));
}
```

### 2.2 protocol：`ModelSummary.input` + `PluginLibraryConfigItem.optionsSource`
`packages/protocol/src/types.ts:223-232` / `:759-774`

```ts
export type ModelSummary = {
  provider: string;
  providerName?: string;
  modelId: string;
  name: string;
  thinkingLevels?: string[];
  input?: Array<"text" | "image">;      // ← 新增（可选，避免牵连 agent-controller）
};

export type PluginLibraryConfigItem = {
  ...
  options?: PluginLibraryConfigOption[];
  /** Select only: well-known dynamic option source. Known sources:
   *  `pi:vision-models` ...  Unknown sources must be ignored (static options/text input). */
  optionsSource?: string;                 // ← 新增
  description?: string;
};
```

DTO 端 `isModelSummary` 接受 `input` 为可选（且元素必须是 `"text"` / `"image"`）；
`isPluginLibraryConfigItem` 接受 `optionsSource` 为非空字符串，并对 `select` 改变 `options` 校验：
有 `optionsSource` 时允许 `options` 缺失/空；没有时仍强制非空数组。

### 2.3 pi-host：`sanitizeConfigItem` 透传 `optionsSource`
`packages/pi-host/src/plugin-library-catalog.ts:71-108`

- 校验 `optionsSource` 必须是 `string`；
- select 类型：若带 `optionsSource`，允许 `options` 缺失/空数组；否则继续要求非空数组。
- 校验通过时把 `optionsSource` 写入 `item`。

### 2.4 前端：动态视觉单选框
`apps/desktop/src/features/plugin-library/PluginLibraryPage.tsx`

新增：
- `useVisionModels(enabled)`（行 67-102）：`hostClient.request("piSettings.get", hostContext(host), null)` 拉一次，
  过滤 `Array.isArray(m.input) && m.input.includes("image")`，
  失败/空返回 `[]`；返回 `ModelSummary[] | null`（`null` 表示加载中）。
- `PluginConfigDialog` 内 `needsVisionModels` 决定是否启用 hook；
- `renderControl(item)` 按以下优先级渲染：
  1. `select` + `optionsSource === "pi:vision-models"`：
     - `null` → 禁用 `<Select>` 占位（标签 `pluginsLoading`）；
     - `length === 0` → 退化为 text 输入框 + `pluginsVisionModelFallbackHelp`；
     - 否则：`<Select>` 选项 = `[{value:"", label:pluginsVisionModelAuto}, ...visionModelOption(m)]`；
       额外保留"已持久化但不在当前列表里的值"作为兜底选项。
  2. `select` + 有静态 `options` → 静态 `<Select>`（不变）；
  3. 其它（含未知 `optionsSource` 且无静态选项）→ 退化为 text 输入框。

`apps/desktop/src/features/plugin-library/plugin-library-model.ts`
- `OPTIONS_SOURCE_VISION_MODELS = "pi:vision-models"`
- `isVisionCapable(model)` / `visionModelOption(model)` / `wantsVisionModelOptions(item)`

### 2.5 i18n
- `pluginsVisionModelAuto`（en/zh）
- `pluginsVisionModelFallbackHelp`（en/zh）

---

## 3. 行为约定（已实现）

- `optionsSource` 未知 → 不报错，按"静态 select / text 输入"回退。
- `PI_VISION_FALLBACK_MODELS` 在 PiDeck 配置界面改为动态列表：使用 `+` / `-` 添加或删除顺序项，选项来自运行时视觉模型；底层仍按英文逗号分隔保存。为兼容旧版插件清单，前端也会按 env 名识别旧的 `text` 配置项。
- `value=""`（自动选择）→ `buildPluginEnvPatch` 与 `pluginLibrary.setEnv` 现成逻辑已不注入（`null`）。
- 不在 host / desktop 任何地方写入模型清单 — 视觉模型完全来自运行时 `piSettings.get`。

---

## 4. 验收对照

| 验收 | 状态 | 说明 |
|------|------|------|
| 相关测试全部通过（含新增 catalog / modelSummaries 用例） | ✅ | host 13 个相关测试、desktop model 18 个测试、DOM 12 个测试均通过；新增回退列表和下拉滚动验收 |
| 手工验证：插件库 → pi-vision → "默认视觉模型"展示"供应商名 · 模型名" | 🟡 待人工 | 需装 pi-vision 并登录多模型供应商 |
| 选中"自动选择"→ 持久化值为空串/不写；选模型 → `PI_VISION_MODEL=provider/modelId` | ✅ 代码已支持 | 由 `buildPluginEnvPatch` + `pluginLibrary.setEnv` 现成路径保证 |

---

## 5. ❗ 待完善（按优先级）

### 5.1 【已完成】前端 DOM 验收测试
**文件**：`apps/desktop/src/features/plugin-library/PluginLibraryPage.dom.test.tsx`

任务里要求的"每个选项显示供应商名 · 模型名 / 文字模型不进选项 / 选自动不注入 / 选模型注入 provider/modelId"
目前**没有 DOM 测试**，只覆盖了纯函数（`plugin-library-model.test.ts`）。

**接续者要做的最小用例**（写在 `describe("PluginLibraryPage DOM workflows", ...)` 内）：

1. **catalog 含 pi-vision repo plugin**：`config: [visionModel select/optionsSource, fallbackModels text]`，对应仓库资源标记为 enabled。
2. **mock `piSettings.get`** 返回模型列表：
   - `{ provider:"openai", providerName:"OpenAI", modelId:"gpt-4o-mini", name:"GPT-4o mini", input:["text","image"] }`
   - `{ provider:"anthropic", providerName:"Anthropic", modelId:"claude-sonnet-45", name:"Claude Sonnet 4.5", input:["text","image"] }`
   - `{ provider:"openai", providerName:"OpenAI", modelId:"gpt-text", name:"GPT Text", input:["text"] }`
3. **打开配置对话框**，点击 `pi-vision` 卡片齿轮按钮（现有 helper 已在）。
4. 等待 `Select` 列表框出现；断言：
   - 列表里包含 `自动选择（...）` / `OpenAI · GPT-4o mini` / `Anthropic · Claude Sonnet 4.5`；
   - 列表里**不**包含 `GPT Text`（纯文本模型）；
   - 列表里**不**包含 `openai/gpt-4o-mini` 这类 raw ID。
5. 点击 `OpenAI · GPT-4o mini` → 保存 → 断言：
   - `desktop_settings_patch` 的 `pluginEnv["pi-vision"].PI_VISION_MODEL === "openai/gpt-4o-mini"`；
   - `pluginLibrary.setEnv` 被调用且 `vars.PI_VISION_MODEL === "openai/gpt-4o-mini"`。
6. 另起一个子用例：选中 `自动选择`（value `""`）→ 保存 → 断言：
   - `pluginEnv["pi-vision"]` 不应被 `desktop_settings_patch` 声明（或被写成 `{}`）；
   - `pluginLibrary.setEnv` 的 `vars.PI_VISION_MODEL === null`。
7. 另起一个子用例：mock `piSettings.get` 抛错（或返回空 models）→ 断言渲染退化为 text 输入框 + 帮助文字 `pluginsVisionModelFallbackHelp`；输入 `anthropic/claude-sonnet-45` 保存 → 注入此值。
8. 另起一个子用例：未知 `optionsSource: "pi:everything"` 的 select → 渲染为 text 输入框、不报错。

**注意**：
- `useAppStore` 的 `setDesktopSettings` 已经被 `tauriMocks.invoke.mockImplementation` 模拟，能让 `persistDesktopSettings` 真正落库。
- `hostClient.request` 在 `beforeEach` 被 `vi.spyOn` 覆盖；新增 `piSettings.get` 分支返回 `{ ok: true, result: { models: [...] } }`。
- `Select` 组件是自渲染的 listbox，用 `await user.click(trigger)` + `await screen.findByRole("option", { name: ... })` 交互。

### 5.2 【已完成】protocol 重建
`@pideck/protocol` 通过 `dist/index.js` 提供，主项目脚本默认会先 build protocol：

```bash
pnpm -F @pideck/protocol build
```

未跑过这条，本次也没人确认 `dist` 已经是最新。**接续者务必先跑一次**，否则下游 vitest 类型解析可能取旧 type。

### 5.3 【已完成/部分阻断】运行测试
```bash
# 仅本任务相关
pnpm -F @pideck/pi-host test -- plugin-library-catalog pi-settings-controller
pnpm -F @pideck/desktop test -- plugin-library-model
# 完整 typecheck（推荐）：按根脚本走
pnpm typecheck
# 完整测试
pnpm test
```

**期望**：
- `plugin-library-catalog.test.ts` 9 套用例全过（已有 7 + 新增 3：optionsSource 接受空 options、空 options 保留 optionsSource、非字符串 optionsSource 拒绝）。
- `pi-settings-controller.test.ts` 2 套用例全过（input 字段透传、缺失时降级 `[]`）。
- `plugin-library-model.test.ts` 既有 6 套 + 新增 3 套（isVisionCapable / visionModelOption / wantsVisionModelOptions）全过。
- 待补充的 §5.1 DOM 测试全过。

### 5.4 【建议】typecheck 走一遍
`@pideck/protocol` 改了 `types.ts`；`@pideck/pi-host` 改了 `pi-settings-controller.ts`；前端改了 `plugin-library-model.ts` 和 `PluginLibraryPage.tsx`。任何一处类型错误会阻断 `pnpm typecheck`，**接续者**先用：
```bash
pnpm -F @pideck/protocol run build
pnpm -F @pideck/pi-host run typecheck
pnpm -F @pideck/desktop run typecheck
```

### 5.5 【可选】前端 KPI 文案统一
`pluginsVisionModelAuto` 现在的中英文文案与上游插件内"auto" 行为保持一致。当前文案：
- zh：`自动选择`
- en：`Automatic (the plugin picks an available vision model)`

当前中文界面使用简洁文案 `自动选择`。

### 5.6 【已完成】配置下拉列表与回退模型列表体验

- `apps/desktop/src/components/Select.tsx` 的 listbox 改为通过 portal 渲染到 `document.body`，使用 fixed 浮层定位；根据触发按钮上下空间自动向上/向下展开，并限制自身高度、启用自身 `overflow-y-auto`，不会撑高或受外层配置弹窗滚动影响。
- `PI_VISION_FALLBACK_MODELS` 使用多个独立 Select 行；`+` 添加下一顺序，`-` 删除当前顺序。
- 选项继续使用运行时视觉模型的「供应商 · 模型名」显示，保存时按顺序拼成 `provider/modelId,provider/modelId`。
- 旧插件清单仍声明为 `text` 时，前端按 `PI_VISION_FALLBACK_MODELS` env 兼容渲染为列表，不要求插件代码先改动。

### 5.7 【可选】未知 optionsSource 的退化
当前 `renderControl` 逻辑：未知 `optionsSource` 且无静态 `options` → 退化 text 输入框。满足"不得报错"。
但若未来上游误用 `optionsSource: "pi:future"` 配了 `options`，会直接当成静态 select 渲染（仍然有效）。文档已说明，
短期内不做更激进的检测。

---

## 6. 已知"

- `rg` 在当前 shell 不可用，全程用 `grep -RIn` / `find` 替代。
- 已注明写 "draft" 的 `ProviderModelConfig.input`（第 321 行）类型本就存在；新 `ModelSummary.input` 与之同义但独立声明，避免影响 `agent-controller#summarizeModel`。
- 工作区还有一批无关改动（features/skills、TelegramAddDialog 等），本次全部跳过未触碰。

---

## 7. 接续 Checklist（最短路径）

1. ✅ 在 `apps/desktop/src/features/plugin-library/PluginLibraryPage.dom.test.tsx` 写 §5.1
   的用例（最关键）。
2. ✅ 跑 `pnpm -F @pideck/protocol build`，确保 `dist` 同步。
3. 🟡 跑 `pnpm -F @pideck/pi-host test`、`pnpm -F @pideck/desktop test`、`pnpm typecheck`。
   定向测试与 typecheck 已通过；全量 `pnpm test` 仍被既有
   `packages/pi-host/src/host.integration.test.ts` 的 host line 超时阻断。
4. ☐ 手工：登录至少一个有视觉模型的供应商 → 插件库 → pi-vision → 配置 → 确认
   选项都是「供应商 · 模型名」、自动选择 = `""` / 选模型 = `provider/modelId`。
5. 如果整体测试受阻，**先暂停并报告** —— 不要在 `agent-controller` /
   `summarizeModel` / `session-snapshot` 上顺手动到 `ModelSummary` 链路以"全部
   required 化"，那是另一个任务。

---

## 8. 关键文件清单（按 diff 大小）

```
packages/protocol/src/types.ts                          类型
packages/protocol/src/dto-validate.ts                    DTO 校验
packages/pi-host/src/pi-settings-controller.ts          输入字段
packages/pi-host/src/plugin-library-catalog.ts           路径校验
packages/pi-host/src/plugin-library-catalog.test.ts      新增 3 用例
packages/pi-host/src/pi-settings-controller.test.ts     新文件（2 用例）
apps/desktop/src/features/plugin-library/plugin-library-model.ts    纯函数
apps/desktop/src/features/plugin-library/plugin-library-model.test.ts  + 3 用例
apps/desktop/src/features/plugin-library/PluginLibraryPage.tsx       动态渲染
apps/desktop/src/lib/i18n/en.ts                          +2 键
apps/desktop/src/lib/i18n/zh.ts                          +2 键
```


原始问题提示词：
# 任务：插件库配置项支持"视觉模型动态单选框"

## 背景

上游插件库（github.com/Nick12138/my-pi-plugins，plugins.json）里的 pi-vision 插件
已把"默认视觉模型"配置项改成如下形式（你可在本仓库插件清单拉取后验证）：

{
  "key": "visionModel",
  "type": "select",
  "label": "默认视觉模型",
  "env": "PI_VISION_MODEL",
  "default": "",
  "optionsSource": "pi:vision-models"
}

需要 PiDeck 侧支持 `optionsSource`：渲染配置表单时，
用本机运行时模型列表动态生成单选框选项（视觉模型是真正的动态数据，
取决于用户 models.json + 已登录 provider，无法在静态清单里写死）。

本仓库已有现成模型查询链路：前端 PiSettings.tsx 调 `piSettings.get`，
pi-host 侧 packages/pi-host/src/pi-settings-controller.ts 的
`modelSummaries(runtime)`（约 41 行）基于 `ModelRuntime.getAvailableSnapshot()`
返回本机可用模型，现已返回 provider / providerName / modelId / name，
缺 input 字段（底层 Model 类型本就有 `input: ("text"|"image")[]`）。

## 改动点（共 3 处）

### 1. pi-host：模型摘要补 input 字段
- 文件：packages/pi-host/src/pi-settings-controller.ts
- `modelSummaries` 返回对象加 `input: model.input ?? []`；
  同步更新 ModelSummary 类型定义
- 更新相关测试（grep modelSummaries / piSettings.get 找到 *.test.ts）

### 2. pi-host：插件库清单校验放宽
- 文件：packages/pi-host/src/plugin-library-catalog.ts（约 76–101 行 ConfigItem 校验）
- ConfigItem 增加可选字段 `optionsSource?: string`，校验通过并透传进清单数据
- select 校验规则：带 `optionsSource` 时允许 options 缺失/为空；
  不带时维持"options 必须非空数组"不变
- plugin-library-catalog.test.ts 加用例：带 optionsSource 且 options 为空的 select 应通过

### 3. 前端：渲染"视觉模型"动态单选框
- 目录：apps/desktop/src/features/plugin-library/（渲染 config 表单的组件）
- 当配置项 `type === "select"` 且 `optionsSource === "pi:vision-models"`：
  1) 调 `piSettings.get` 拿 result.models（写法参考 PiSettings.tsx 50–65 行）
  2) 只保留视觉模型：过滤 `Array.isArray(m.input) && m.input.includes("image")`
  3) 选项显示为「供应商名称 + 模型名称」，禁止显示 providerId/modelId：
     ```
     options = [
       { value: "", label: "自动选择" },
       ...visionModels.map(m => ({
         value: `${m.provider}/${m.modelId}`,           // 注入 env 仍用 ID
         label: `${m.providerName ?? m.provider} · ${m.name ?? m.modelId}`,
       })),
     ]
     ```
     例如显示 "OpenAI · GPT-4o mini"、"Anthropic · Claude Sonnet 4.5"，
     而不是 "openai/gpt-4o-mini"。
  4) 请求失败 / 视觉模型为 0 个时降级为 text 输入框，help text：
     "未检测到可用视觉模型，可手动填写 provider/modelId（需在 models.json 中配置且 input 含 image）"
- 选中值照常持久化并以 PI_VISION_MODEL 环境变量注入；value="" 时不注入（让插件走 auto）
- 插件代码一行不改

## 约定
- optionsSource 预留扩展（未来 pi:all-models 等）；未知 optionsSource 一律按原
  text/静态 select 行为渲染，不得报错
- 回退模型列表 PI_VISION_FALLBACK_MODELS 维持 text 类型不动
- 上游 plugins.json 已就绪，不要在 PiDeck 仓库里放任何模型清单硬编码

## 验收
1. 相关测试全部通过（含新增 catalog / modelSummaries 用例）
2. 手工验证：插件库 → pi-vision → 配置表单 "默认视觉模型" 为单选下拉，
   每个选项显示「供应商名称 · 模型名称」且全部是视觉模型（input 含 image）；
   文字模型（如纯文本对话模型）不得出现在选项里
3. 选 "自动选择" 后清单持久化的值为空串/不写；选一个模型后注入 PI_VISION_MODEL=provider/modelId
