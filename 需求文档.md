1.所有的单选按钮打开的选择项，需要增加内边距，以及边框幅度；
2.设置-模型服务-添加按钮，点击则直接打开自定义的添加模型服务界面，不要再选择一次；因为下面内置账号登陆，功能重复
3.对话区宽度，自动宽度
4.对话区显示混乱，不够清晰
5.系统指定角色，是什么意思
6.对话框中的模型选择，我想要用模型的显示名称以及模型名称来显示，目前是服务ID/模型名称；对话框的思考深度，需要改成图标+思考等级；目前只是一个图标；
7.分析一下目前上传附件，是怎么搞得？只是将路径加进去了，还是说处理了什么事儿？需要研究一下docx以及pdf文档；另外需要增加文件夹、快捷键的附件；
8.OpenAI 兼容性设置，这个好像没怎么设置过，最好是自动适配，然后删除这个设置选项；
9.显示名称跟服务ID为什么有2个，我只想有一个；
10.删除高级端点（模型列表）的配置
11.编辑模型的时候，模型的推理改成大脑图标，图片增加图片的图标、增加上下文大小的显示，比如200k
12.文本的行间距需要增加一点，不然看着太乱了；
13.增加对话重试按钮；
14.subagent需要开发
15.自动压缩，需要配置具体多少压缩；
16.通知 英文问题；需要转换成中文
17.右侧面板的新增按钮，有遮挡问题
18.改动，增加自动填充提交说明，如果有git，则自动增加一个悬浮窗，用来提交或查看改动。
19.会话数，改成使用会话界面中切换，并且点击之后，不要在填充输入框，不要回到新对话界面，只定位位置；
20.搜索按钮中活跃和已归档的大小不一样；删除按钮，改成归档按钮；
21.增加文件索引功能（不急）；
22.技能，（重要）包管理，好像就是这个功能
23.插件、工具：包管理，好像就是这个功能
24.agent性格；
25.左上角PI的图标，增加鼠标聚焦改成分栏的图标；并且点击则分栏，删除侧边栏的箭头按钮；
26.消息，改成右侧对齐；
27.不显示新对话 就绪，这几个字
28.新对话的欢迎词，自动修改提示词；
29.界面缩放
30.工作区同时运行功能
31.最近对话，按时间排序，今天，本周，更早；
32.会话标题，需要使用emoji开头；每个会话标题必须用一个emoji


---

## 设计原则：单界面、不再使用二级覆盖界面

应用整体保持**单主界面**结构，不再新增任何会盖住主界面的二级覆盖层（Overlay / 弹窗 / 全屏遮罩 / 独立设置页等）。

- **现状要改的**：当前"设置"是通过 `SettingsOverlay`（`absolute inset-0 z-40`）整层覆盖主界面的，这是要去掉的二级覆盖界面。
- **未来的约束**：即便后续新增任何配置/管理类功能（例如 Packages、Provider、扩展面板等），也一律在主界面内承载——通过切换主对话区的显示内容、或替换 `<main>` 里渲染的组件来实现，而**不再叠一层覆盖在主界面上**。侧栏、顶部对话头、右 Dock 等主界面元素在所有视图下都应保持可见可交互。
- **例外**：必要的轻量临时遮挡（确认对话框、菜单、tooltip、拖放提示）不在此约束范围内，它们是临时浮层、不是常驻视图。

---

## 设置入口与显示位置改造

### 目标
把"设置"从当前"侧栏底部入口 + 全屏覆盖主界面"的形式，改成"侧栏『新对话』下方入口 + 在右边对话消息流区域内显示设置内容"。让用户调整设置时，侧栏 / 顶部对话头 / 右 Dock 仍可见可交互，不再被整层遮罩盖住。本改造是上述"单界面、不二级覆盖"原则的第一例落地。

### 现状（已查证）
- 入口：唯一入口在侧栏最底部，`apps/desktop/src/components/Sidebar.tsx:171-194`，点击调用 `setPage(page === "chat" ? "settings" : "chat")`。
- 显示方式：通过 Zustand store 的 `page: NavPage`（`apps/desktop/src/lib/stores/app-store.ts:58`，取值 `"chat" | "packages" | "settings"`）切换视图。
  - `page === "settings"` 时，在应用根 div 最上层渲染 `SettingsOverlay`（`apps/desktop/src/app/App.tsx:55-112`），用 `absolute inset-0 z-40` **覆盖整个应用界面**（侧栏 + 主对话区 + 右 Dock 全部被盖）。
  - 主对话区 `<main>`（`apps/desktop/src/app/App.tsx:1046-1059`）里渲染 `ChatPage`（`apps/desktop/src/features/chat/ChatPage.tsx`），内部是 `Transcript`（消息流）+ `Composer`（输入框）。
  - 设置页根本没走主对话区那条渲染链，是另起一层叠在上面，这才是它"覆盖了外面的对话界面"的根因。
- 设置页本体：`apps/desktop/src/features/settings/SettingsPage.tsx`，已经是块状布局（左分区导航 + 右表单），自带返回按钮与 `onClose` prop（`SettingsPage.tsx:396`）——复用度极高。
- Packages 页：`NavPage` 里的 `"packages"` 目前也走同一个 Overlay，section 设为 `"packages"` 后由 `SettingsPage` 内部渲染 `PackagesPage`（`App.tsx:1064`）。

### 改造方案

#### 1. 入口迁移：侧栏底部 → "新对话"下方
- 在 `Sidebar.tsx:131-135`（包裹 `NewSessionButton` 的容器）下方新增一个"设置"按钮，复用现有样式与点击逻辑（`setPage("settings")`）。
- 删除（或归整）`Sidebar.tsx:171-194` 底部那段设置按钮。
- 待定 / 需求方拍板点：
  - Packages 页是否顺着一起内嵌进主对话区、还是单独拆出来？（见下方"Packages 页的处理"。）

#### 2. 显示方式改造：取消全屏 Overlay，改在主对话区显示
- 改 `App.tsx:1046-1059` 的 `<main>` 思路：按 `page` 分流——
  - `page === "settings"` → 渲染 `<SettingsPage initialSection={settingsSection} onClose={() => setPage("chat")} />`
  - 否则 → 维持 `<ChatPage />` 不变
- 删除 `App.tsx:1062-1066` 的 `SettingsOverlay` 条件渲染、`App.tsx:55-112` 的 `SettingsOverlay` 组件本身、`App.tsx:528` 的 `settingsOverlayOpen` 派生状态。
- 迁移 `App.tsx:96-109` 里 Overlay 的 Esc 关闭 + providersDirty 丢弃确认对话框逻辑到 `SettingsPage` 内部（或 ChatPage sibling 处理），否则改设置时按 Esc 会丢、且 Esc 关不了设置。

#### 3. Packages 页的处理
- 按上述"单界面"原则，Packages 也应内嵌进主对话区，不再走 `SettingsOverlay`/独立覆盖层（改动最小、与设置行为一致）。"
- 若后续 Packages 功能膨胀到需要更大空间，做法应是扩展主界面布局（例如占用主对话区并复用分区导航），而不是退回 Overlay。

### 影响面盘点
| 文件 | 改动 |
|---|---|
| `apps/desktop/src/components/Sidebar.tsx` | 删底部设置按钮（`:171-194`），在"新对话"容器下（`:131-135`）新增设置按钮。 |
| `apps/desktop/src/app/App.tsx` | 删 `SettingsOverlay`（`:55-112`）、删其挂载（`:1062-1066`）、删 `settingsOverlayOpen`（`:528`）；改 `<main>`（`:1046-1059`）按 `page` 分流；Esc + 脏数据确认逻辑迁移。 |
| `apps/desktop/src/lib/stores/app-store.ts` | `NavPage` / `setPage` / `openSettingsSection` 基本不动（语义不变，消费方变）。`settingsSection` 默认值语义可选确认。 |
| `apps/desktop/src/features/settings/SettingsPage.tsx` | 几乎不动；可选地把 Esc / 脏数据确认逻辑内化进来。 |
| `apps/desktop/src/lib/i18n/zh.ts` / `en.ts` | 不动，文案 key 复用 `settingsTitle`。 |

### 可行性结论
- 能实现，工作量中等偏小。核心改动集中在 `App.tsx` 视图分流一处 + `Sidebar.tsx` 入口搬迁一处，`SettingsPage` 复用度高。
- 体验上比现在好：调设置时侧栏、右 Dock 仍可见可交互，与"工作区"概念更贴。
- Packages 一并内嵌进主对话区（按"单界面"原则，不再保留独立 Overlay/覆盖层）。
