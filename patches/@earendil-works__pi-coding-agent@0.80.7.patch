diff --git a/dist/core/extensions/loader.js b/dist/core/extensions/loader.js
index 575a208f581631bd98561b36ada62e31c235eb56..be5e786751d3fe636bfc4b32d73e839316293917 100644
--- a/dist/core/extensions/loader.js
+++ b/dist/core/extensions/loader.js
@@ -112,9 +112,11 @@ export function clearExtensionCache() {
 }
 function useExtensionCacheCwd(cwd) {
     const resolvedCwd = resolvePath(cwd);
-    if (extensionCacheCwd !== undefined && extensionCacheCwd !== resolvedCwd) {
-        clearExtensionCache();
-    }
+    // PiDeck patch: keep the extension module cache across cwd changes.
+    // Cache keys are absolute extension paths and factories receive cwd at
+    // invocation time, so user-scope package modules (identical for every
+    // workspace) load once per process instead of once per workspace switch.
+    // Package install/update still calls clearExtensionCache() explicitly.
     extensionCacheCwd = resolvedCwd;
     return { cwd: resolvedCwd, generation: extensionCacheGeneration };
 }
diff --git a/dist/core/resource-loader.js b/dist/core/resource-loader.js
--- a/dist/core/resource-loader.js
+++ b/dist/core/resource-loader.js
@@ -216,6 +216,6 @@ export class DefaultResourceLoader {
     async reload(options) {
         resetTimings("extensions");
-        if (this.loaded) {
+        if (this.loaded && !options?.preserveExtensionCache) {
             clearExtensionCache();
         }
         let preTrustExtensions;
diff --git a/dist/core/resource-loader.d.ts b/dist/core/resource-loader.d.ts
--- a/dist/core/resource-loader.d.ts
+++ b/dist/core/resource-loader.d.ts
@@ -24,5 +24,6 @@ export interface ResourceLoaderReloadOptions {
         extensionsResult: LoadExtensionsResult;
     }) => Promise<boolean>;
+    preserveExtensionCache?: boolean;
 }
 export interface ResourceLoader {
     getExtensions(): LoadExtensionsResult;
diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -2026,7 +2026,9 @@ export class AgentSession {
         await this.settingsManager.reload();
         this.syncQueueModesFromSettings();
         resetApiProviders();
-        await this._resourceLoader.reload();
+        await this._resourceLoader.reload({
+            preserveExtensionCache: options?.preserveExtensionCache,
+        });
         this._buildRuntime({
             activeToolNames: this.getActiveToolNames(),
             flagValues: previousFlagValues,
diff --git a/dist/core/agent-session.d.ts b/dist/core/agent-session.d.ts
--- a/dist/core/agent-session.d.ts
+++ b/dist/core/agent-session.d.ts
@@ -498,6 +498,7 @@ export declare class AgentSession {
     private _bindExtensionCore;
     private _refreshToolRegistry;
     private _buildRuntime;
     reload(options?: {
         beforeSessionStart?: () => void | Promise<void>;
+        preserveExtensionCache?: boolean;
     }): Promise<void>;
diff --git a/dist/core/package-manager.d.ts b/dist/core/package-manager.d.ts
--- a/dist/core/package-manager.d.ts
+++ b/dist/core/package-manager.d.ts
@@ -61,7 +61,8 @@ export interface PackageManager {
     removeSourceFromSettings(source: string, options?: {
         local?: boolean;
     }): boolean;
     setProgressCallback(callback: ProgressCallback | undefined): void;
+    setOperationSignal?(signal: AbortSignal | undefined): void;
     getInstalledPath(source: string, scope: "user" | "project"): string | undefined;
 }
 interface PackageManagerOptions {
@@ -80,8 +81,10 @@ export declare class DefaultPackageManager implements PackageManager {
     private globalNpmRoot;
     private globalNpmRootCommandKey;
     private progressCallback;
+    private operationSignal;
     constructor(options: PackageManagerOptions);
     setProgressCallback(callback: ProgressCallback | undefined): void;
+    setOperationSignal(signal: AbortSignal | undefined): void;
     addSourceToSettings(source: string, options?: {
         local?: boolean;
     }): boolean;
diff --git a/dist/core/package-manager.js b/dist/core/package-manager.js
--- a/dist/core/package-manager.js
+++ b/dist/core/package-manager.js
@@ -603,6 +603,7 @@ export class DefaultPackageManager {
     globalNpmRoot;
     globalNpmRootCommandKey;
     progressCallback;
+    operationSignal;
     constructor(options) {
         this.cwd = resolvePath(options.cwd);
         this.agentDir = resolvePath(options.agentDir);
@@ -612,6 +613,9 @@ export class DefaultPackageManager {
     setProgressCallback(callback) {
         this.progressCallback = callback;
     }
+    setOperationSignal(signal) {
+        this.operationSignal = signal;
+    }
     addSourceToSettings(source, options) {
         const scope = options?.local ? "project" : "user";
         const currentSettings = scope === "project" ? this.settingsManager.getProjectSettings() : this.settingsManager.getGlobalSettings();
@@ -2058,6 +2062,7 @@ export class DefaultPackageManager {
             cwd: options?.cwd,
             stdio: isStdoutTakenOver() ? ["ignore", 2, 2] : "inherit",
             env,
+            signal: this.operationSignal,
         });
     }
     spawnCaptureCommand(command, args, options) {
@@ -2067,6 +2072,7 @@ export class DefaultPackageManager {
             cwd: options?.cwd,
             stdio: ["ignore", "pipe", "pipe"],
             env,
+            signal: this.operationSignal,
         });
     }
     runCommandCapture(command, args, options) {
