diff --git a/dist/core/agent-session.d.ts b/dist/core/agent-session.d.ts
index e5d84dd..06503e9 100644
--- a/dist/core/agent-session.d.ts
+++ b/dist/core/agent-session.d.ts
@@ -16,7 +16,7 @@ import type { Agent, AgentEvent, AgentMessage, AgentState, AgentTool, ThinkingLe
 import type { ImageContent, Model, TextContent } from "@earendil-works/pi-ai/compat";
 import { type BashResult } from "./bash-executor.ts";
 import { type CompactionResult } from "./compaction/index.ts";
-import { type ContextUsage, type ExtensionCommandContextActions, type ExtensionErrorListener, type ExtensionMode, ExtensionRunner, type ExtensionUIContext, type InputSource, type ReplacedSessionContext, type SessionStartEvent, type ShutdownHandler, type ToolDefinition, type ToolInfo } from "./extensions/index.ts";
+import { type ContextUsage, type ExtensionCommandContextActions, type ExtensionErrorListener, type ExtensionInvocationRunner, type ExtensionMode, ExtensionRunner, type ExtensionUIContext, type InputSource, type ReplacedSessionContext, type SessionStartEvent, type ShutdownHandler, type ToolDefinition, type ToolInfo } from "./extensions/index.ts";
 import type { CustomMessage } from "./messages.ts";
 import type { ModelRuntime } from "./model-runtime.ts";
 import { type PromptTemplate } from "./prompt-templates.ts";
@@ -145,6 +145,7 @@ export interface AgentSessionConfig {
 export interface ExtensionBindings {
     uiContext?: ExtensionUIContext;
     mode?: ExtensionMode;
+    invocationRunner?: ExtensionInvocationRunner;
     commandContextActions?: ExtensionCommandContextActions;
     abortHandler?: () => void;
     shutdownHandler?: ShutdownHandler;
@@ -439,6 +440,11 @@ export declare class AgentSession {
      * @throws Error if no auth is configured for the model
      */
     setModel(model: Model<any>): Promise<void>;
+    /**
+     * Clear the active model without changing the persisted default.
+     * Used when an embedder intentionally has no enabled Provider.
+     */
+    clearModel(): Promise<void>;
     /**
      * Cycle to next/previous model.
      * Uses scoped models (from --models flag) if available, otherwise all available models.
diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index 4bcf0d4..c8f92cd 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -73,6 +73,19 @@ function estimateMessagesTokens(messages) {
 // ============================================================================
 /** Standard thinking levels */
 const THINKING_LEVELS = ["off", "minimal", "low", "medium", "high"];
+/** Safe sentinel used while the application has no enabled Provider. */
+const NO_MODEL = Object.freeze({
+    id: "unknown",
+    name: "unknown",
+    api: "unknown",
+    provider: "unknown",
+    baseUrl: "",
+    reasoning: false,
+    input: [],
+    cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 },
+    contextWindow: 0,
+    maxTokens: 0,
+});
 // ============================================================================
 // AgentSession Class
 // ============================================================================
@@ -120,6 +133,7 @@ export class AgentSession {
     _sessionStartEvent;
     _extensionUIContext;
     _extensionMode = "print";
+    _extensionInvocationRunner;
     _extensionCommandContextActions;
     _extensionAbortHandler;
     _extensionShutdownHandler;
@@ -1207,6 +1221,16 @@ export class AgentSession {
         this.setThinkingLevel(thinkingLevel);
         await this._emitModelSelect(model, previousModel, "set");
     }
+    /**
+     * Clear the active model without changing the persisted default. This lets
+     * embedders represent an intentional state with no enabled Provider.
+     */
+    async clearModel() {
+        const previousModel = this.model;
+        this.agent.state.model = NO_MODEL;
+        this.setThinkingLevel("off");
+        await this._emitModelSelect(NO_MODEL, previousModel, "set");
+    }
     /**
      * Cycle to next/previous model.
      * Uses scoped models (from --models flag) if available, otherwise all available models.
@@ -1745,6 +1769,9 @@ export class AgentSession {
         if (bindings.mode !== undefined) {
             this._extensionMode = bindings.mode;
         }
+        if (bindings.invocationRunner !== undefined) {
+            this._extensionInvocationRunner = bindings.invocationRunner;
+        }
         if (bindings.commandContextActions !== undefined) {
             this._extensionCommandContextActions = bindings.commandContextActions;
         }
@@ -1803,6 +1830,7 @@ export class AgentSession {
     }
     _applyExtensionBindings(runner) {
         runner.setUIContext(this._extensionUIContext, this._extensionMode);
+        runner.setInvocationRunner(this._extensionInvocationRunner);
         runner.bindCommandContext(this._extensionCommandContextActions);
         this._extensionErrorUnsubscriber?.();
         this._extensionErrorUnsubscriber = this._extensionErrorListener
@@ -2064,6 +2092,7 @@ export class AgentSession {
             includeAllExtensionTools: true,
         });
         const hasBindings = this._extensionUIContext ||
+            this._extensionInvocationRunner ||
             this._extensionCommandContextActions ||
             this._extensionShutdownHandler ||
             this._extensionErrorListener;
diff --git a/dist/core/extensions/index.d.ts b/dist/core/extensions/index.d.ts
index 901326b..2baaf4e 100644
--- a/dist/core/extensions/index.d.ts
+++ b/dist/core/extensions/index.d.ts
@@ -6,7 +6,7 @@ export type { SourceInfo } from "../source-info.ts";
 export { createExtensionRuntime, discoverAndLoadExtensions, loadExtensionFromFactory, loadExtensions, } from "./loader.ts";
 export type { ExtensionErrorListener, ForkHandler, NavigateTreeHandler, NewSessionHandler, ShutdownHandler, SwitchSessionHandler, } from "./runner.ts";
 export { ExtensionRunner } from "./runner.ts";
-export type { AfterProviderResponseEvent, AgentEndEvent, AgentSettledEvent, AgentStartEvent, AgentToolResult, AgentToolUpdateCallback, AppendEntryHandler, AppKeybinding, AutocompleteProviderFactory, BashToolCallEvent, BashToolResultEvent, BeforeAgentStartEvent, BeforeAgentStartEventResult, BeforeProviderHeadersEvent, BeforeProviderRequestEvent, BeforeProviderRequestEventResult, BuildSystemPromptOptions, CompactOptions, ContextEvent, ContextEventResult, ContextUsage, CustomToolCallEvent, CustomToolResultEvent, EditorFactory, EditToolCallEvent, EditToolResultEvent, EntryRenderer, EntryRenderOptions, ExecOptions, ExecResult, Extension, ExtensionActions, ExtensionAPI, ExtensionCommandContext, ExtensionCommandContextActions, ExtensionContext, ExtensionContextActions, ExtensionError, ExtensionEvent, ExtensionFactory, ExtensionFlag, ExtensionHandler, ExtensionMode, ExtensionRuntime, ExtensionShortcut, ExtensionUIContext, ExtensionUIDialogOptions, ExtensionWidgetOptions, FindToolCallEvent, FindToolResultEvent, GetActiveToolsHandler, GetAllToolsHandler, GetCommandsHandler, GetThinkingLevelHandler, GrepToolCallEvent, GrepToolResultEvent, InlineExtension, InputEvent, InputEventResult, InputSource, KeybindingsManager, LoadExtensionsResult, LsToolCallEvent, LsToolResultEvent, MarkdownTransformContext, MarkdownTransformer, MessageEndEvent, MessageRenderer, MessageRenderOptions, MessageStartEvent, MessageUpdateEvent, ModelSelectEvent, ModelSelectSource, ProjectTrustContext, ProjectTrustEvent, ProjectTrustEventDecision, ProjectTrustEventResult, ProjectTrustHandler, ProviderConfig, ProviderModelConfig, ReadToolCallEvent, ReadToolResultEvent, RegisteredCommand, RegisteredTool, ReplacedSessionContext, ResolvedCommand, ResourcesDiscoverEvent, ResourcesDiscoverResult, SendMessageHandler, SendUserMessageHandler, SessionBeforeCompactEvent, SessionBeforeCompactResult, SessionBeforeForkEvent, SessionBeforeForkResult, SessionBeforeSwitchEvent, SessionBeforeSwitchResult, SessionBeforeTreeEvent, SessionBeforeTreeResult, SessionCompactEvent, SessionEvent, SessionInfoChangedEvent, SessionShutdownEvent, SessionStartEvent, SessionTreeEvent, SetActiveToolsHandler, SetLabelHandler, SetModelHandler, SetThinkingLevelHandler, TerminalInputHandler, ToolCallEvent, ToolCallEventResult, ToolDefinition, ToolExecutionEndEvent, ToolExecutionMode, ToolExecutionStartEvent, ToolExecutionUpdateEvent, ToolInfo, ToolRenderResultOptions, ToolResultEvent, ToolResultEventResult, TreePreparation, TurnEndEvent, TurnStartEvent, UserBashEvent, UserBashEventResult, WidgetPlacement, WorkingIndicatorOptions, WriteToolCallEvent, WriteToolResultEvent, } from "./types.ts";
+export type { AfterProviderResponseEvent, AgentEndEvent, AgentSettledEvent, AgentStartEvent, AgentToolResult, AgentToolUpdateCallback, AppendEntryHandler, AppKeybinding, AutocompleteProviderFactory, BashToolCallEvent, BashToolResultEvent, BeforeAgentStartEvent, BeforeAgentStartEventResult, BeforeProviderHeadersEvent, BeforeProviderRequestEvent, BeforeProviderRequestEventResult, BuildSystemPromptOptions, CompactOptions, ContextEvent, ContextEventResult, ContextUsage, CustomToolCallEvent, CustomToolResultEvent, EditorFactory, EditToolCallEvent, EditToolResultEvent, EntryRenderer, EntryRenderOptions, ExecOptions, ExecResult, Extension, ExtensionActions, ExtensionAPI, ExtensionCommandContext, ExtensionCommandContextActions, ExtensionContext, ExtensionContextActions, ExtensionError, ExtensionEvent, ExtensionFactory, ExtensionFlag, ExtensionHandler, ExtensionInvocationMetadata, ExtensionInvocationRunner, ExtensionMode, ExtensionRuntime, ExtensionShortcut, ExtensionUIContext, ExtensionUIDialogOptions, ExtensionWidgetOptions, FindToolCallEvent, FindToolResultEvent, GetActiveToolsHandler, GetAllToolsHandler, GetCommandsHandler, GetThinkingLevelHandler, GrepToolCallEvent, GrepToolResultEvent, InlineExtension, InputEvent, InputEventResult, InputSource, KeybindingsManager, LoadExtensionsResult, LsToolCallEvent, LsToolResultEvent, MarkdownTransformContext, MarkdownTransformer, MessageEndEvent, MessageRenderer, MessageRenderOptions, MessageStartEvent, MessageUpdateEvent, ModelSelectEvent, ModelSelectSource, ProjectTrustContext, ProjectTrustEvent, ProjectTrustEventDecision, ProjectTrustEventResult, ProjectTrustHandler, ProviderConfig, ProviderModelConfig, ReadToolCallEvent, ReadToolResultEvent, RegisteredCommand, RegisteredTool, ReplacedSessionContext, ResolvedCommand, ResourcesDiscoverEvent, ResourcesDiscoverResult, SendMessageHandler, SendUserMessageHandler, SessionBeforeCompactEvent, SessionBeforeCompactResult, SessionBeforeForkEvent, SessionBeforeForkResult, SessionBeforeSwitchEvent, SessionBeforeSwitchResult, SessionBeforeTreeEvent, SessionBeforeTreeResult, SessionCompactEvent, SessionEvent, SessionInfoChangedEvent, SessionShutdownEvent, SessionStartEvent, SessionTreeEvent, SetActiveToolsHandler, SetLabelHandler, SetModelHandler, SetThinkingLevelHandler, TerminalInputHandler, ToolCallEvent, ToolCallEventResult, ToolDefinition, ToolExecutionEndEvent, ToolExecutionMode, ToolExecutionStartEvent, ToolExecutionUpdateEvent, ToolInfo, ToolRenderResultOptions, ToolResultEvent, ToolResultEventResult, TreePreparation, TurnEndEvent, TurnStartEvent, UserBashEvent, UserBashEventResult, WidgetPlacement, WorkingIndicatorOptions, WriteToolCallEvent, WriteToolResultEvent, } from "./types.ts";
 export { defineTool, isBashToolResult, isEditToolResult, isFindToolResult, isGrepToolResult, isLsToolResult, isReadToolResult, isToolCallEventType, isWriteToolResult, } from "./types.ts";
 export { wrapRegisteredTool, wrapRegisteredTools } from "./wrapper.ts";
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/dist/core/extensions/runner.d.ts b/dist/core/extensions/runner.d.ts
index 9b579db..e9292f8 100644
--- a/dist/core/extensions/runner.d.ts
+++ b/dist/core/extensions/runner.d.ts
@@ -9,7 +9,7 @@ import type { KeybindingsConfig } from "../keybindings.ts";
 import type { ModelRegistry } from "../model-registry.ts";
 import type { SessionManager } from "../session-manager.ts";
 import type { BuildSystemPromptOptions } from "../system-prompt.ts";
-import type { BeforeAgentStartEvent, BeforeAgentStartEventResult, BeforeProviderHeadersEvent, BeforeProviderRequestEvent, ContextEvent, EntryRenderer, Extension, ExtensionActions, ExtensionCommandContext, ExtensionCommandContextActions, ExtensionContext, ExtensionContextActions, ExtensionError, ExtensionEvent, ExtensionFlag, ExtensionMode, ExtensionRuntime, ExtensionShortcut, ExtensionUIContext, InputEvent, InputEventResult, InputSource, LoadExtensionsResult, MarkdownTransformer, MessageEndEvent, MessageRenderer, ProjectTrustContext, ProjectTrustEvent, ProjectTrustEventResult, ProviderConfig, RegisteredTool, ReplacedSessionContext, ResolvedCommand, ResourcesDiscoverEvent, SessionBeforeCompactResult, SessionBeforeForkResult, SessionBeforeSwitchResult, SessionBeforeTreeResult, SessionShutdownEvent, ToolCallEvent, ToolCallEventResult, ToolResultEvent, ToolResultEventResult, UserBashEvent, UserBashEventResult } from "./types.ts";
+import type { BeforeAgentStartEvent, BeforeAgentStartEventResult, BeforeProviderHeadersEvent, BeforeProviderRequestEvent, ContextEvent, EntryRenderer, Extension, ExtensionActions, ExtensionCommandContext, ExtensionCommandContextActions, ExtensionContext, ExtensionContextActions, ExtensionError, ExtensionEvent, ExtensionFlag, ExtensionInvocationMetadata, ExtensionInvocationRunner, ExtensionMode, ExtensionRuntime, ExtensionShortcut, ExtensionUIContext, InputEvent, InputEventResult, InputSource, LoadExtensionsResult, MarkdownTransformer, MessageEndEvent, MessageRenderer, ProjectTrustContext, ProjectTrustEvent, ProjectTrustEventResult, ProviderConfig, RegisteredTool, ReplacedSessionContext, ResolvedCommand, ResourcesDiscoverEvent, SessionBeforeCompactResult, SessionBeforeForkResult, SessionBeforeSwitchResult, SessionBeforeTreeResult, SessionShutdownEvent, ToolCallEvent, ToolCallEventResult, ToolResultEvent, ToolResultEventResult, UserBashEvent, UserBashEventResult } from "./types.ts";
 /** Combined result from all before_agent_start handlers */
 interface BeforeAgentStartCombinedResult {
     messages?: NonNullable<BeforeAgentStartEventResult["message"]>[];
@@ -72,6 +72,7 @@ export declare class ExtensionRunner {
     private runtime;
     private uiContext;
     private mode;
+    private invocationRunner;
     private cwd;
     private sessionManager;
     private modelRegistry;
@@ -105,6 +106,8 @@ export declare class ExtensionRunner {
     }): void;
     bindCommandContext(actions?: ExtensionCommandContextActions): void;
     setUIContext(uiContext?: ExtensionUIContext, mode?: ExtensionMode): void;
+    setInvocationRunner(invocationRunner?: ExtensionInvocationRunner): void;
+    invokeExtension<T>(metadata: ExtensionInvocationMetadata, invoke: () => T | Promise<T>): T | Promise<T>;
     getUIContext(): ExtensionUIContext;
     hasUI(): boolean;
     getExtensionPaths(): string[];
@@ -143,6 +146,7 @@ export declare class ExtensionRunner {
     createContext(): ExtensionContext;
     createCommandContext(): ExtensionCommandContext;
     private isSessionBeforeEvent;
+    private invokeExtensionEvent;
     emit<TEvent extends RunnerEmitEvent>(event: TEvent): Promise<RunnerEmitResult<TEvent>>;
     emitMessageEnd(event: MessageEndEvent): Promise<AgentMessage | undefined>;
     emitToolResult(event: ToolResultEvent): Promise<ToolResultEventResult | undefined>;
diff --git a/dist/core/extensions/runner.js b/dist/core/extensions/runner.js
index 418865a..860290b 100644
--- a/dist/core/extensions/runner.js
+++ b/dist/core/extensions/runner.js
@@ -122,6 +122,7 @@ export class ExtensionRunner {
     runtime;
     uiContext;
     mode = "print";
+    invocationRunner = (_metadata, invoke) => invoke();
     cwd;
     sessionManager;
     modelRegistry;
@@ -268,6 +269,12 @@ export class ExtensionRunner {
         this.uiContext = uiContext ?? noOpUIContext;
         this.mode = mode;
     }
+    setInvocationRunner(invocationRunner) {
+        this.invocationRunner = invocationRunner ?? ((_metadata, invoke) => invoke());
+    }
+    invokeExtension(metadata, invoke) {
+        return this.invocationRunner(metadata, invoke);
+    }
     getUIContext() {
         return this.uiContext;
     }
@@ -576,6 +583,17 @@ export class ExtensionRunner {
             event.type === "session_before_compact" ||
             event.type === "session_before_tree");
     }
+    invokeExtensionEvent(ext, event, invoke) {
+        const toolName = typeof event.toolName === "string" ? event.toolName : undefined;
+        const toolCallId = typeof event.toolCallId === "string" ? event.toolCallId : undefined;
+        return this.invokeExtension({
+            kind: "event",
+            sourceInfo: ext.sourceInfo,
+            eventType: event.type,
+            ...(toolName === undefined ? {} : { toolName }),
+            ...(toolCallId === undefined ? {} : { toolCallId }),
+        }, invoke);
+    }
     async emit(event) {
         const ctx = this.createContext();
         let result;
@@ -585,7 +603,7 @@ export class ExtensionRunner {
                 continue;
             for (const handler of handlers) {
                 try {
-                    const handlerResult = await handler(event, ctx);
+                    const handlerResult = await this.invokeExtensionEvent(ext, event, () => handler(event, ctx));
                     if (this.isSessionBeforeEvent(event) && handlerResult) {
                         result = handlerResult;
                         if (result.cancel) {
@@ -618,7 +636,7 @@ export class ExtensionRunner {
             for (const handler of handlers) {
                 try {
                     const currentEvent = { ...event, message: currentMessage };
-                    const handlerResult = (await handler(currentEvent, ctx));
+                    const handlerResult = (await this.invokeExtensionEvent(ext, currentEvent, () => handler(currentEvent, ctx)));
                     if (!handlerResult?.message)
                         continue;
                     if (handlerResult.message.role !== currentMessage.role) {
@@ -656,7 +674,7 @@ export class ExtensionRunner {
                 continue;
             for (const handler of handlers) {
                 try {
-                    const handlerResult = (await handler(currentEvent, ctx));
+                    const handlerResult = (await this.invokeExtensionEvent(ext, currentEvent, () => handler(currentEvent, ctx)));
                     if (!handlerResult)
                         continue;
                     if (handlerResult.content !== undefined) {
@@ -706,7 +724,7 @@ export class ExtensionRunner {
             if (!handlers || handlers.length === 0)
                 continue;
             for (const handler of handlers) {
-                const handlerResult = await handler(event, ctx);
+                const handlerResult = await this.invokeExtensionEvent(ext, event, () => handler(event, ctx));
                 if (handlerResult) {
                     result = handlerResult;
                     if (result.block) {
@@ -725,7 +743,7 @@ export class ExtensionRunner {
                 continue;
             for (const handler of handlers) {
                 try {
-                    const handlerResult = await handler(event, ctx);
+                    const handlerResult = await this.invokeExtensionEvent(ext, event, () => handler(event, ctx));
                     if (handlerResult) {
                         return handlerResult;
                     }
@@ -754,7 +772,7 @@ export class ExtensionRunner {
             for (const handler of handlers) {
                 try {
                     const event = { type: "context", messages: currentMessages };
-                    const handlerResult = await handler(event, ctx);
+                    const handlerResult = await this.invokeExtensionEvent(ext, event, () => handler(event, ctx));
                     if (handlerResult && handlerResult.messages) {
                         currentMessages = handlerResult.messages;
                     }
@@ -786,7 +804,7 @@ export class ExtensionRunner {
                         type: "before_provider_request",
                         payload: currentPayload,
                     };
-                    const handlerResult = await handler(event, ctx);
+                    const handlerResult = await this.invokeExtensionEvent(ext, event, () => handler(event, ctx));
                     if (handlerResult !== undefined) {
                         currentPayload = handlerResult;
                     }
@@ -818,7 +836,7 @@ export class ExtensionRunner {
                         type: "before_provider_headers",
                         headers,
                     };
-                    await handler(event, ctx);
+                    await this.invokeExtensionEvent(ext, event, () => handler(event, ctx));
                 }
                 catch (err) {
                     const message = err instanceof Error ? err.message : String(err);
@@ -856,7 +874,7 @@ export class ExtensionRunner {
                         systemPrompt: currentSystemPrompt,
                         systemPromptOptions,
                     };
-                    const handlerResult = await handler(event, ctx);
+                    const handlerResult = await this.invokeExtensionEvent(ext, event, () => handler(event, ctx));
                     if (handlerResult) {
                         const result = handlerResult;
                         if (result.message) {
@@ -900,7 +918,7 @@ export class ExtensionRunner {
             for (const handler of handlers) {
                 try {
                     const event = { type: "resources_discover", cwd, reason };
-                    const handlerResult = await handler(event, ctx);
+                    const handlerResult = await this.invokeExtensionEvent(ext, event, () => handler(event, ctx));
                     const result = handlerResult;
                     if (result?.skillPaths?.length) {
                         skillPaths.push(...result.skillPaths.map((path) => ({ path, extensionPath: ext.path })));
@@ -941,7 +959,7 @@ export class ExtensionRunner {
                         source,
                         streamingBehavior,
                     };
-                    const result = (await handler(event, ctx));
+                    const result = (await this.invokeExtensionEvent(ext, event, () => handler(event, ctx)));
                     if (result?.action === "handled")
                         return result;
                     if (result?.action === "transform") {
diff --git a/dist/core/extensions/types.d.ts b/dist/core/extensions/types.d.ts
index 556623b..c0e09d4 100644
--- a/dist/core/extensions/types.d.ts
+++ b/dist/core/extensions/types.d.ts
@@ -33,12 +33,44 @@ export type { BuildSystemPromptOptions } from "../system-prompt.ts";
 export type { AgentToolResult, AgentToolUpdateCallback, ToolExecutionMode };
 export type { AppKeybinding, KeybindingsManager } from "../keybindings.ts";
 /** Options for extension UI dialogs. */
+/** Optional PiDeck-only presentation metadata. Other UI modes ignore it. */
+export interface PiDeckExtensionUIDialogOptions {
+    presentation?: "inline" | "modal";
+    sourceLabel?: string;
+    correlationId?: string;
+    risk?: "normal" | "high";
+    allowFreeform?: boolean;
+    optionDetails?: Array<{
+        id: string;
+        description?: string;
+        destructive?: boolean;
+    }>;
+}
+/** Options for extension UI dialogs. */
 export interface ExtensionUIDialogOptions {
     /** AbortSignal to programmatically dismiss the dialog. */
     signal?: AbortSignal;
     /** Timeout in milliseconds. Dialog auto-dismisses with live countdown display. */
     timeout?: number;
+    /** Declarative presentation hints used by PiDeck's desktop UI. */
+    pideck?: PiDeckExtensionUIDialogOptions;
 }
+/** Host-neutral metadata for one Extension callback invocation. */
+export type ExtensionInvocationMetadata = {
+    kind: "tool";
+    sourceInfo: SourceInfo;
+    toolName: string;
+    toolCallId: string;
+    signal?: AbortSignal;
+} | {
+    kind: "event";
+    sourceInfo: SourceInfo;
+    eventType: string;
+    toolName?: string;
+    toolCallId?: string;
+};
+/** Optional Host wrapper used to propagate trusted Extension invocation context. */
+export type ExtensionInvocationRunner = <T>(metadata: ExtensionInvocationMetadata, invoke: () => T | Promise<T>) => T | Promise<T>;
 /** Placement for extension widgets. */
 export type WidgetPlacement = "aboveEditor" | "belowEditor";
 /** Options for extension widgets. */
@@ -132,7 +164,7 @@ export interface ExtensionUIContext {
     /** Get the current text from the core input editor. */
     getEditorText(): string;
     /** Show a multi-line editor for text editing. */
-    editor(title: string, prefill?: string): Promise<string | undefined>;
+    editor(title: string, prefill?: string, opts?: ExtensionUIDialogOptions): Promise<string | undefined>;
     /** Stack additional autocomplete behavior on top of the built-in provider. */
     addAutocompleteProvider(factory: AutocompleteProviderFactory): void;
     /**
diff --git a/dist/core/extensions/wrapper.js b/dist/core/extensions/wrapper.js
index 1e9f713..77d6039 100644
--- a/dist/core/extensions/wrapper.js
+++ b/dist/core/extensions/wrapper.js
@@ -14,7 +14,13 @@ export function wrapRegisteredTool(registeredTool, runner) {
     const execute = tool.execute;
     return {
         ...tool,
-        execute: async (toolCallId, params, signal, onUpdate) => {
+        execute: (toolCallId, params, signal, onUpdate) => runner.invokeExtension({
+            kind: "tool",
+            sourceInfo: registeredTool.sourceInfo,
+            toolName: registeredTool.definition.name,
+            toolCallId,
+            signal,
+        }, async () => {
             const activeBefore = runner.getActiveTools();
             const result = await execute(toolCallId, params, signal, onUpdate);
             const activeAfter = runner.getActiveTools();
@@ -28,7 +34,7 @@ export function wrapRegisteredTool(registeredTool, runner) {
                 ...result,
                 addedToolNames: [...new Set([...(result.addedToolNames ?? []), ...addedToolNames])],
             };
-        },
+        }),
     };
 }
 /**
diff --git a/dist/core/package-manager.d.ts b/dist/core/package-manager.d.ts
index 14030e7..05fce68 100644
--- a/dist/core/package-manager.d.ts
+++ b/dist/core/package-manager.d.ts
@@ -50,7 +50,9 @@ export interface PackageManager {
     removeAndPersist(source: string, options?: {
         local?: boolean;
     }): Promise<boolean>;
-    update(source?: string): Promise<void>;
+    update(source?: string, options?: {
+        local?: boolean;
+    }): Promise<void>;
     listConfiguredPackages(): ConfiguredPackage[];
     resolveExtensionSources(sources: string[], options?: {
         local?: boolean;
@@ -63,6 +65,12 @@ export interface PackageManager {
         local?: boolean;
     }): boolean;
     setProgressCallback(callback: ProgressCallback | undefined): void;
+    /**
+     * PiDeck patch: required so callers cannot silently skip cancellation.
+     * Applies to the async npm/git spawn paths; `runCommandSync` can only
+     * refuse to start after abort.
+     */
+    setOperationSignal(signal: AbortSignal | undefined): void;
     getInstalledPath(source: string, scope: "user" | "project"): string | undefined;
 }
 interface PackageManagerOptions {
@@ -79,8 +87,15 @@ export declare class DefaultPackageManager implements PackageManager {
     private globalNpmRoot;
     private globalNpmRootCommandKey;
     private progressCallback;
+    private operationSignal;
     constructor(options: PackageManagerOptions);
     setProgressCallback(callback: ProgressCallback | undefined): void;
+    /**
+     * PiDeck patch: required so callers cannot silently skip cancellation.
+     * Applies to the async npm/git spawn paths; `runCommandSync` can only
+     * refuse to start after abort.
+     */
+    setOperationSignal(signal: AbortSignal | undefined): void;
     addSourceToSettings(source: string, options?: {
         local?: boolean;
     }): boolean;
@@ -108,7 +123,9 @@ export declare class DefaultPackageManager implements PackageManager {
     removeAndPersist(source: string, options?: {
         local?: boolean;
     }): Promise<boolean>;
-    update(source?: string): Promise<void>;
+    update(source?: string, options?: {
+        local?: boolean;
+    }): Promise<void>;
     private updateConfiguredSources;
     private shouldUpdateNpmSource;
     private updateNpmBatch;
diff --git a/dist/core/package-manager.js b/dist/core/package-manager.js
index e806af3..1540e29 100644
--- a/dist/core/package-manager.js
+++ b/dist/core/package-manager.js
@@ -26,10 +26,9 @@ import ignore from "ignore";
 import { minimatch } from "minimatch";
 import { maxSatisfying, rcompare, satisfies, valid, validRange } from "semver";
 import { CONFIG_DIR_NAME } from "../config.js";
-import { spawnProcess, spawnProcessSync } from "../utils/child-process.js";
+import { spawnProcess, spawnProcessSync, waitForChildProcess } from "../utils/child-process.js";
 import { parseGitUrl } from "../utils/git.js";
 import { canonicalizePath, isLocalPath, markPathIgnoredByCloudSync, resolvePath } from "../utils/paths.js";
-import { isStdoutTakenOver } from "./output-guard.js";
 import { readPiManifest } from "./pi-manifest.js";
 const NETWORK_TIMEOUT_MS = 10000;
 const UPDATE_CHECK_CONCURRENCY = 4;
@@ -594,6 +593,7 @@ export class DefaultPackageManager {
     globalNpmRoot;
     globalNpmRootCommandKey;
     progressCallback;
+    operationSignal;
     constructor(options) {
         this.cwd = resolvePath(options.cwd);
         this.agentDir = resolvePath(options.agentDir);
@@ -602,6 +602,12 @@ export class DefaultPackageManager {
     setProgressCallback(callback) {
         this.progressCallback = callback;
     }
+    // PiDeck patch: let the Host cancel npm/git children. Upstream spawns them
+    // with no signal, so an aborted package operation leaves the child running
+    // and holds the graph lock until it finishes on its own.
+    setOperationSignal(signal) {
+        this.operationSignal = signal;
+    }
     addSourceToSettings(source, options) {
         const scope = options?.local ? "project" : "user";
         const currentSettings = scope === "project" ? this.settingsManager.getProjectSettings() : this.settingsManager.getGlobalSettings();
@@ -798,13 +804,15 @@ export class DefaultPackageManager {
         await this.remove(source, options);
         return this.removeSourceFromSettings(source, options);
     }
-    async update(source) {
+    async update(source, options) {
+        const requestedScope = source && options ? (options.local ? "project" : "user") : undefined;
         const globalSettings = this.settingsManager.getGlobalSettings();
         const projectSettings = this.settingsManager.getProjectSettings();
         const identity = source ? this.getPackageIdentity(source) : undefined;
         let matched = false;
         const updateSources = [];
         for (const pkg of globalSettings.packages ?? []) {
+            if (requestedScope === "project") continue;
             const sourceStr = typeof pkg === "string" ? pkg : pkg.source;
             if (identity && this.getPackageIdentity(sourceStr, "user") !== identity)
                 continue;
@@ -812,6 +820,7 @@ export class DefaultPackageManager {
             updateSources.push({ source: sourceStr, scope: "user" });
         }
         for (const pkg of projectSettings.packages ?? []) {
+            if (requestedScope === "user") continue;
             const sourceStr = typeof pkg === "string" ? pkg : pkg.source;
             if (identity && this.getPackageIdentity(sourceStr, "project") !== identity)
                 continue;
@@ -2087,8 +2096,9 @@ export class DefaultPackageManager {
         const env = getEnv();
         return spawnProcess(command, args, {
             cwd: options?.cwd,
-            stdio: isStdoutTakenOver() ? ["ignore", 2, 2] : "inherit",
+            stdio: ["ignore", "pipe", "pipe"],
             env,
+            signal: this.operationSignal,
         });
     }
     spawnCaptureCommand(command, args, options) {
@@ -2098,62 +2108,65 @@ export class DefaultPackageManager {
             cwd: options?.cwd,
             stdio: ["ignore", "pipe", "pipe"],
             env,
+            signal: this.operationSignal,
         });
     }
-    runCommandCapture(command, args, options) {
-        return new Promise((resolvePromise, reject) => {
-            const child = this.spawnCaptureCommand(command, args, options);
-            let stdout = "";
-            let stderr = "";
-            let timedOut = false;
-            const timeout = typeof options?.timeoutMs === "number"
-                ? setTimeout(() => {
-                    timedOut = true;
-                    child.kill();
-                }, options.timeoutMs)
-                : undefined;
-            child.stdout?.on("data", (data) => {
-                stdout += data.toString();
-            });
-            child.stderr?.on("data", (data) => {
-                stderr += data.toString();
-            });
-            child.once("error", (error) => {
-                if (timeout)
-                    clearTimeout(timeout);
-                reject(error);
-            });
-            child.once("close", (code, signal) => {
-                if (timeout)
-                    clearTimeout(timeout);
-                if (timedOut) {
-                    reject(new Error(`${command} ${args.join(" ")} timed out after ${options?.timeoutMs}ms`));
-                    return;
-                }
-                if (code === 0) {
-                    resolvePromise(stdout.trim());
-                    return;
-                }
-                const exitStatus = code === null ? `signal ${signal ?? "unknown"}` : `code ${code}`;
-                reject(new Error(`${command} ${args.join(" ")} failed with ${exitStatus}: ${stderr || stdout}`));
-            });
+    async runCommandCapture(command, args, options) {
+        const child = this.spawnCaptureCommand(command, args, options);
+        let stdout = "";
+        let stderr = "";
+        let timedOut = false;
+        const timeout = typeof options?.timeoutMs === "number"
+            ? setTimeout(() => {
+                timedOut = true;
+                child.kill();
+            }, options.timeoutMs)
+            : undefined;
+        child.stdout?.on("data", (data) => {
+            stdout += data.toString();
+        });
+        child.stderr?.on("data", (data) => {
+            stderr += data.toString();
         });
+        let code;
+        try {
+            code = await waitForChildProcess(child);
+        }
+        finally {
+            if (timeout)
+                clearTimeout(timeout);
+        }
+        if (timedOut) {
+            throw new Error(`${command} ${args.join(" ")} timed out after ${options?.timeoutMs}ms`);
+        }
+        if (code === 0) {
+            return stdout.trim();
+        }
+        const exitStatus = code === null ? `signal ${child.signalCode ?? "unknown"}` : `code ${code}`;
+        throw new Error(`${command} ${args.join(" ")} failed with ${exitStatus}: ${stderr || stdout}`);
     }
-    runCommand(command, args, options) {
-        return new Promise((resolvePromise, reject) => {
-            const child = this.spawnCommand(command, args, options);
-            child.on("error", reject);
-            child.on("exit", (code) => {
-                if (code === 0) {
-                    resolvePromise();
-                }
-                else {
-                    reject(new Error(`${command} ${args.join(" ")} failed with code ${code}`));
-                }
-            });
+    async runCommand(command, args, options) {
+        const child = this.spawnCommand(command, args, options);
+        let stdout = "";
+        let stderr = "";
+        child.stdout?.on("data", (data) => {
+            stdout = `${stdout}${data.toString()}`.slice(-65536);
         });
+        child.stderr?.on("data", (data) => {
+            stderr = `${stderr}${data.toString()}`.slice(-65536);
+        });
+        const code = await waitForChildProcess(child);
+        if (code === 0)
+            return;
+        const exitStatus = code === null ? `signal ${child.signalCode ?? "unknown"}` : `code ${code}`;
+        const output = (stderr || stdout).trim();
+        throw new Error(`${command} ${args.join(" ")} failed with ${exitStatus}${output ? `: ${output}` : ""}`);
     }
     runCommandSync(command, args) {
+        // PiDeck patch: spawnSync cannot be interrupted, so an in-flight sync
+        // child still runs to completion. Refusing to start a new one after
+        // abort is the only cancellation this path can honour.
+        this.operationSignal?.throwIfAborted();
         const env = getEnv();
         const result = spawnProcessSync(command, args, {
             stdio: ["ignore", "pipe", "pipe"],
diff --git a/dist/core/resource-loader.js b/dist/core/resource-loader.js
index 97af6cd..1a64d6f 100644
--- a/dist/core/resource-loader.js
+++ b/dist/core/resource-loader.js
@@ -296,6 +296,7 @@ export class DefaultResourceLoader {
         const enabledSkillResources = getEnabledResources(resolvedPaths.skills);
         const enabledPrompts = getEnabledPaths(resolvedPaths.prompts);
         const enabledThemes = getEnabledPaths(resolvedPaths.themes);
+        this._resolvedMetadataByPath = metadataByPath;
         const enabledSkills = enabledSkillResources.map((resource) => this.mapSkillPath(resource, metadataByPath));
         // Add CLI paths metadata
         for (const r of cliExtensionPaths.extensions) {
@@ -512,7 +513,7 @@ export class DefaultResourceLoader {
         const resolvedSkills = this.skillsOverride ? this.skillsOverride(skillsResult) : skillsResult;
         this.skills = resolvedSkills.skills.map((skill) => ({
             ...skill,
-            sourceInfo: this.findSourceInfoForPath(skill.filePath, this.extensionSkillSourceInfos, metadataByPath) ??
+            sourceInfo: this.findSourceInfoForPath(skill.filePath, this.extensionSkillSourceInfos, metadataByPath ?? this._resolvedMetadataByPath) ??
                 skill.sourceInfo ??
                 this.getDefaultSourceInfoForPath(skill.filePath),
         }));
@@ -535,7 +536,7 @@ export class DefaultResourceLoader {
         const resolvedPrompts = this.promptsOverride ? this.promptsOverride(promptsResult) : promptsResult;
         this.prompts = resolvedPrompts.prompts.map((prompt) => ({
             ...prompt,
-            sourceInfo: this.findSourceInfoForPath(prompt.filePath, this.extensionPromptSourceInfos, metadataByPath) ??
+            sourceInfo: this.findSourceInfoForPath(prompt.filePath, this.extensionPromptSourceInfos, metadataByPath ?? this._resolvedMetadataByPath) ??
                 prompt.sourceInfo ??
                 this.getDefaultSourceInfoForPath(prompt.filePath),
         }));
@@ -555,7 +556,7 @@ export class DefaultResourceLoader {
         this.themes = resolvedThemes.themes.map((theme) => {
             const sourcePath = theme.sourcePath;
             theme.sourceInfo = sourcePath
-                ? (this.findSourceInfoForPath(sourcePath, this.extensionThemeSourceInfos, metadataByPath) ??
+                ? (this.findSourceInfoForPath(sourcePath, this.extensionThemeSourceInfos, metadataByPath ?? this._resolvedMetadataByPath) ??
                     theme.sourceInfo ??
                     this.getDefaultSourceInfoForPath(sourcePath))
                 : theme.sourceInfo;
diff --git a/dist/core/sdk.d.ts b/dist/core/sdk.d.ts
index df0c5c1..d5e890f 100644
--- a/dist/core/sdk.d.ts
+++ b/dist/core/sdk.d.ts
@@ -15,7 +15,8 @@ export interface CreateAgentSessionOptions {
     /** Canonical model/auth runtime. Defaults to a runtime using agentDir/auth.json and models.json. */
     modelRuntime?: ModelRuntime;
     /** Model to use. Default: from settings, else first available */
-    model?: Model<any>;
+    /** Model to use. Default: from settings, else first available. Pass null to disable automatic selection. */
+    model?: Model<any> | null;
     /** Thinking level. Default: from settings, else 'medium' (clamped to model capabilities) */
     thinkingLevel?: ThinkingLevel;
     /** Models available for cycling (Ctrl+P in interactive mode) */
diff --git a/dist/core/sdk.js b/dist/core/sdk.js
index 8c8d049..1c98c74 100644
--- a/dist/core/sdk.js
+++ b/dist/core/sdk.js
@@ -81,10 +81,11 @@ export async function createAgentSession(options = {}) {
     const existingSession = sessionManager.buildSessionContext();
     const hasExistingSession = existingSession.messages.length > 0;
     const hasThinkingEntry = sessionManager.getBranch().some((entry) => entry.type === "thinking_level_change");
-    let model = options.model;
+    const autoSelectModel = options.model !== null;
+    let model = options.model ?? undefined;
     let modelFallbackMessage;
     // If session has data, try to restore model from it
-    if (!model && hasExistingSession && existingSession.model) {
+    if (autoSelectModel && !model && hasExistingSession && existingSession.model) {
         const restoredModel = modelRuntime.getModel(existingSession.model.provider, existingSession.model.modelId);
         if (restoredModel && modelRuntime.hasConfiguredAuth(restoredModel.provider)) {
             model = restoredModel;
@@ -94,7 +95,7 @@ export async function createAgentSession(options = {}) {
         }
     }
     // If still no model, use findInitialModel (checks settings default, then provider defaults)
-    if (!model) {
+    if (autoSelectModel && !model) {
         const result = await findInitialModel({
             scopedModels: [],
             isContinuing: hasExistingSession,
diff --git a/dist/index.d.ts b/dist/index.d.ts
index f43bc96..685f87d 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -4,7 +4,7 @@ export { AgentSession, type AgentSessionConfig, type AgentSessionEvent, type Age
 export { readStoredCredential } from "./core/auth-storage.ts";
 export { type BranchPreparation, type BranchSummaryResult, type CollectEntriesResult, type CompactionResult, type CutPointResult, calculateContextTokens, collectEntriesForBranchSummary, compact, DEFAULT_COMPACTION_SETTINGS, estimateTokens, type FileOperations, findCutPoint, findTurnStartIndex, type GenerateBranchSummaryOptions, generateBranchSummary, generateSummary, generateSummaryWithUsage, getLastAssistantUsage, prepareBranchEntries, serializeConversation, shouldCompact, } from "./core/compaction/index.ts";
 export { createEventBus, type EventBus, type EventBusController } from "./core/event-bus.ts";
-export type { AgentEndEvent, AgentSettledEvent, AgentStartEvent, AgentToolResult, AgentToolUpdateCallback, AppKeybinding, AutocompleteProviderFactory, BashToolCallEvent, BeforeAgentStartEvent, BeforeAgentStartEventResult, BeforeProviderHeadersEvent, BeforeProviderRequestEvent, BeforeProviderRequestEventResult, BuildSystemPromptOptions, CompactOptions, ContextEvent, ContextUsage, CustomToolCallEvent, EditToolCallEvent, EntryRenderer, EntryRenderOptions, ExecOptions, ExecResult, Extension, ExtensionActions, ExtensionAPI, ExtensionCommandContext, ExtensionCommandContextActions, ExtensionContext, ExtensionContextActions, ExtensionError, ExtensionEvent, ExtensionFactory, ExtensionFlag, ExtensionHandler, ExtensionRuntime, ExtensionShortcut, ExtensionUIContext, ExtensionUIDialogOptions, ExtensionWidgetOptions, FindToolCallEvent, GrepToolCallEvent, InlineExtension, InputEvent, InputEventResult, InputSource, KeybindingsManager, LoadExtensionsResult, LsToolCallEvent, MarkdownTransformContext, MarkdownTransformer, MessageEndEvent, MessageRenderer, MessageRenderOptions, MessageStartEvent, MessageUpdateEvent, ProjectTrustContext, ProjectTrustEvent, ProjectTrustEventDecision, ProjectTrustEventResult, ProjectTrustHandler, ProviderConfig, ProviderModelConfig, ReadToolCallEvent, RegisteredCommand, RegisteredTool, ResolvedCommand, SessionBeforeCompactEvent, SessionBeforeForkEvent, SessionBeforeSwitchEvent, SessionBeforeTreeEvent, SessionCompactEvent, SessionInfoChangedEvent, SessionShutdownEvent, SessionStartEvent, SessionTreeEvent, SlashCommandInfo, SlashCommandSource, SourceInfo, TerminalInputHandler, ToolCallEvent, ToolCallEventResult, ToolDefinition, ToolExecutionEndEvent, ToolExecutionMode, ToolExecutionStartEvent, ToolExecutionUpdateEvent, ToolInfo, ToolRenderResultOptions, ToolResultEvent, TurnEndEvent, TurnStartEvent, UserBashEvent, UserBashEventResult, WidgetPlacement, WorkingIndicatorOptions, WriteToolCallEvent, } from "./core/extensions/index.ts";
+export type { AgentEndEvent, AgentSettledEvent, AgentStartEvent, AgentToolResult, AgentToolUpdateCallback, AppKeybinding, AutocompleteProviderFactory, BashToolCallEvent, BeforeAgentStartEvent, BeforeAgentStartEventResult, BeforeProviderHeadersEvent, BeforeProviderRequestEvent, BeforeProviderRequestEventResult, BuildSystemPromptOptions, CompactOptions, ContextEvent, ContextUsage, CustomToolCallEvent, EditToolCallEvent, EntryRenderer, EntryRenderOptions, ExecOptions, ExecResult, Extension, ExtensionActions, ExtensionAPI, ExtensionCommandContext, ExtensionCommandContextActions, ExtensionContext, ExtensionContextActions, ExtensionError, ExtensionEvent, ExtensionFactory, ExtensionFlag, ExtensionHandler, ExtensionInvocationMetadata, ExtensionInvocationRunner, ExtensionRuntime, ExtensionShortcut, ExtensionUIContext, ExtensionUIDialogOptions, ExtensionWidgetOptions, FindToolCallEvent, GrepToolCallEvent, InlineExtension, InputEvent, InputEventResult, InputSource, KeybindingsManager, LoadExtensionsResult, LsToolCallEvent, MarkdownTransformContext, MarkdownTransformer, MessageEndEvent, MessageRenderer, MessageRenderOptions, MessageStartEvent, MessageUpdateEvent, ProjectTrustContext, ProjectTrustEvent, ProjectTrustEventDecision, ProjectTrustEventResult, ProjectTrustHandler, ProviderConfig, ProviderModelConfig, ReadToolCallEvent, RegisteredCommand, RegisteredTool, ResolvedCommand, SessionBeforeCompactEvent, SessionBeforeForkEvent, SessionBeforeSwitchEvent, SessionBeforeTreeEvent, SessionCompactEvent, SessionInfoChangedEvent, SessionShutdownEvent, SessionStartEvent, SessionTreeEvent, SlashCommandInfo, SlashCommandSource, SourceInfo, TerminalInputHandler, ToolCallEvent, ToolCallEventResult, ToolDefinition, ToolExecutionEndEvent, ToolExecutionMode, ToolExecutionStartEvent, ToolExecutionUpdateEvent, ToolInfo, ToolRenderResultOptions, ToolResultEvent, TurnEndEvent, TurnStartEvent, UserBashEvent, UserBashEventResult, WidgetPlacement, WorkingIndicatorOptions, WriteToolCallEvent, } from "./core/extensions/index.ts";
 export { createExtensionRuntime, defineTool, discoverAndLoadExtensions, ExtensionRunner, isBashToolResult, isEditToolResult, isFindToolResult, isGrepToolResult, isLsToolResult, isReadToolResult, isToolCallEventType, isWriteToolResult, wrapRegisteredTool, wrapRegisteredTools, } from "./core/extensions/index.ts";
 export type { ReadonlyFooterDataProvider } from "./core/footer-data-provider.ts";
 export { convertToLlm } from "./core/messages.ts";
diff --git a/dist/utils/shell.js b/dist/utils/shell.js
index 68e7e51..a8ddebd 100644
--- a/dist/utils/shell.js
+++ b/dist/utils/shell.js
@@ -1,7 +1,27 @@
 import { existsSync } from "node:fs";
-import { delimiter } from "node:path";
+import { delimiter, dirname, join } from "node:path";
 import { spawn, spawnSync } from "child_process";
 import { getBinDir } from "../config.js";
+function bundledBashFallback() {
+    const bash = process.env.PIDECK_BUNDLED_BASH;
+    if (bash && existsSync(bash)) {
+        return bash;
+    }
+    const git = process.env.PIDECK_BUNDLED_GIT;
+    if (!git) {
+        return null;
+    }
+    const cmdDir = dirname(git);
+    if (cmdDir.split(/[\\/]/).pop()?.toLowerCase() !== "cmd") {
+        return null;
+    }
+    const candidate = join(dirname(cmdDir), "bin", "bash.exe");
+    return existsSync(candidate) ? candidate : null;
+}
+function windowsTaskkillExecutable() {
+    const root = process.env.SystemRoot || process.env.SYSTEMROOT || "C:\\Windows";
+    return join(root, "System32", "taskkill.exe");
+}
 /**
  * Find bash executable on PATH (cross-platform)
  */
@@ -53,7 +73,8 @@ function findBashOnPath() {
  * Resolution order:
  * 1. User-specified shellPath
  * 2. On Windows: Git Bash in known locations, then bash on PATH
- * 3. On Unix: /bin/bash, then bash on PATH, then fallback to sh
+ * 3. On Windows: PiDeck bundled bash (PIDECK_BUNDLED_BASH / Portable Git)
+ * 4. On Unix: /bin/bash, then bash on PATH, then fallback to sh
  */
 export function getShellConfig(customShellPath) {
     // 1. Check user-specified shell path
@@ -84,6 +105,10 @@ export function getShellConfig(customShellPath) {
         if (bashOnPath) {
             return getBashShellConfig(bashOnPath);
         }
+        const bundledBash = bundledBashFallback();
+        if (bundledBash) {
+            return getBashShellConfig(bundledBash);
+        }
         throw new Error(`No bash shell found. Options:\n` +
             `  1. Install Git for Windows: https://git-scm.com/download/win\n` +
             `  2. Add your bash to PATH (Cygwin, MSYS2, etc.)\n` +
@@ -171,16 +196,30 @@ export function killTrackedDetachedChildren() {
  */
 export function killProcessTree(pid) {
     if (process.platform === "win32") {
-        // Use taskkill on Windows to kill process tree
+        // Use absolute taskkill so a user PATH without System32 cannot
+        // turn ENOENT into an unhandled spawn error that kills the Host.
         try {
-            spawn("taskkill", ["/F", "/T", "/PID", String(pid)], {
+            const killer = spawn(windowsTaskkillExecutable(), ["/F", "/T", "/PID", String(pid)], {
                 stdio: "ignore",
                 detached: true,
                 windowsHide: true,
             });
+            killer.once("error", () => {
+                try {
+                    process.kill(pid);
+                }
+                catch {
+                    // Process already dead
+                }
+            });
         }
         catch {
-            // Ignore errors if taskkill fails
+            try {
+                process.kill(pid);
+            }
+            catch {
+                // Ignore errors if taskkill fails
+            }
         }
     }
     else {
